a = 20) * N

det sprowadz dane(): for i in range (N):

a[i] =? int (input("Podaj liczby: "))

pierwsze

zbiore jo

def wyprowadz dane () = for i in range (N): print (a[i]

det maks vybor (starti): maksi = starti maks = a[maksi) for i in range(starti+ 1): it a[i]> mats: maks = a[maksi] autumn maksi

det

sort wybor():

for i in range (N-1): maksi maks wybor (i)- t = alil a[i] = a[maksil almaksi] = t

wprowadz dane ()

sort wybor ( print ("Dane posortowane metodą przez wybieranie")

wyprowadz dane ()

input("InInNaciśnij Enter, aby zakończyć")
